spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springboot?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: root
    initial-size: 10
    max-active: 200
    min-idle: 10
    max-wait: 10
mybatis:
  type-aliases-package: com.enjoy.entity
  mapper-locations: classpath:mapper/*.xml